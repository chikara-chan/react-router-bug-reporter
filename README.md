# react server render

``` bash
npm run dev     # run in development environment
                # the result shows /server/middlewares/clientRoute `match` is sync


npm run build   # bundle all client and server files
npm run start   # start production environment
                # the result shows /server/middlewares/clientRoute `match` is async

```
